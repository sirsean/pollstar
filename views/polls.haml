
%h2
    %a{ :href => "/user/#{@user.username}/" }="#{@user.full_name}"
    ="/ polls"

%p
    %a{ :href => "/user/#{@user.username}/polls/feed/" }="rss feed"

- if @polls.count > 0
    = list_polls(@polls)
- else
    %p
        %em="no polls yet"
